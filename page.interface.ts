
/**
 * @interface IPage
 * Represents an interface for the page data model
 * in the backend.
 */
export interface IPage {
    size: number;
    totalElements: number;
    totalPages: number;
    number: number;
  }
  